/**
 * `true` for non-production bundles; otherwise `false`
 * @internal
 */
export const DEBUG = process.env.NODE_ENV !== "production";

/**
 * Short name for this library (`cqa`)
 * @internal
 */
export const LIB_NAME_SHORT = "cqa";

/**
 * Long name for this library (`Command Query App`)
 * @internal
 */
export const LIB_NAME_LONG = "Command Query App";
